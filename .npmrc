# pnpm v11 只从 .npmrc 读 auth / registry 配置；其余 kebab-case 设置会被**静默忽略**。
# 因此所有非 auth 设置（allowBuilds / overrides / hoist 等）一律写在 pnpm-workspace.yaml，
# 并使用 camelCase。不要把 allow-builds.* / shamefully-hoist 之类加回本文件——写了也不生效。
# 参考：pnpm-workspace.yaml 的 `allowBuilds:` 与 `overrides:` 段。
