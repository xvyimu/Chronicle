# CH-A11Y-SMOKE · 壳层语义/焦点 · 2026-07-24

> Worktree：`ch-a11y-smoke` · Branch：`xvyimu/ch-a11y-smoke`  
> 模块：M-CH-a11y-smoke · WEEK-BACKLOG W8  
> 只改边界：header/footer/theme/back-to-top 装饰 SVG · skip-link 焦点 · 相关 vitest  
> 未动：视觉大洗 · CSP · 栈 · IA · 主内容区大改

## 扫描结论（基线已具备）

| 项                                   | 基线              | 本波增量                                              |
| ------------------------------------ | ----------------- | ----------------------------------------------------- |
| skip-link → `#main-content`          | `layout.tsx` 已有 | 契约测试 + `:focus-visible` 样式                      |
| `lang="zh-CN"`                       | 已有              | 契约测试                                              |
| 主导航 `aria-label` / `aria-current` | Header 已有       | 保留                                                  |
| MobileNav 焦点 / Escape / backdrop   | 已有              | 保留                                                  |
| SearchBar combobox / listbox / live  | 已有              | 保留                                                  |
| 装饰 SVG `aria-hidden`               | 部分缺失          | Header logo / Footer logo+GH icon / ThemeToggle icons |
| ThemeToggle 可访问名                 | 固定「切换主题」  | 状态名：`主题：浅色/深色/跟随系统`                    |
| BackToTop 隐藏时焦点                 | 仅 CSS 隐藏       | `tabIndex=-1` + `aria-hidden`                         |

## 代码 diff 摘要

| 文件                                        | 变更                                             |
| ------------------------------------------- | ------------------------------------------------ |
| `src/components/layout/Header.tsx`          | brand logo `aria-hidden` + svg `focusable=false` |
| `src/components/layout/Footer.tsx`          | logo + GitHub icon 装饰隐藏                      |
| `src/components/ui/ThemeToggle.tsx`         | 状态化 aria-label；icon `aria-hidden`            |
| `src/components/ui/BackToTop.tsx`           | 不可见时退出 Tab 序与 AT                         |
| `src/app/styles/base.css`                   | skip-link `:focus-visible` 与 `:focus` 对齐      |
| `src/app/layout.a11y.test.tsx`              | **new** skip-link / lang / focus 契约            |
| `Header/Footer/ThemeToggle/BackToTop` tests | 对齐上述行为                                     |

## 验证（命令 + exit）

| #   | Command          | Exit  | Notes                                      |
| --- | ---------------- | ----- | ------------------------------------------ |
| 1   | `pnpm typecheck` | **0** | `tsc --noEmit`                             |
| 2   | `pnpm test`      | **0** | Vitest **99** files / **755** tests passed |

Node v24.16.0 vs engines `22.x` → pnpm engine **WARN only**。

## 不做 / 残留

- 全站 axe/Lighthouse a11y 扫（需 e2e 环境；本波 vitest 契约）
- 视觉/IA 大改、CSP 放宽、换栈
- 正文 prose 标题层级深度审计（另波）
